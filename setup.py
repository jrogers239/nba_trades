from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Take Raw Text file an convert into a useable table to track trades',
    author='Jonathan Rogers',
    license='MIT',
)
